# `arkgate` compatibility package

ArkGate was renamed to **Structrail**. Install `structrail` for new projects.

This package keeps the v2 imports, subpaths, and six CLI names working throughout Structrail v3.
It delegates to the exact matching `structrail` version and will not be removed before v4.

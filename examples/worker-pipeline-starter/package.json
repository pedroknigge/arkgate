{
  "name": "ark-gallery-worker-pipeline-starter",
  "private": true,
  "version": "0.0.0",
  "description": "Phase-1 scaffold for background jobs and workers (worker-pipeline archetype)",
  "type": "module",
  "scripts": {
    "check": "ark-check --root . --config ark.config.json --strict-config"
  },
  "devDependencies": {
    "arkgate": "^3.7.0"
  }
}

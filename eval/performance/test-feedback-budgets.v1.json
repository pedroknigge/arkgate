{
  "schemaVersion": 1,
  "runner": { "image": "ubuntu-latest", "node": "20" },
  "method": "Z07 pure canonical-facts tests use parallel threads while the bounded process-adapter list uses one serial fork; hook-path benchmark parity runs in the dedicated performance job and the full lane, while the PR lane retains resident transport plus canonical scale coverage. Package isolation remains blocking in the build job instead of being duplicated in this edit-loop budget. Both lanes start together and the reported metric is total wall time.",
  "modes": {
    "pr": { "maxWallMs": 10000 },
    "full": { "maxWallMs": 30000 }
  }
}

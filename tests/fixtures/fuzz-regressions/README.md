# Fuzz regression corpus

Each minimized counterexample found by a V02 property or fuzz campaign belongs in an
area-specific directory here and must be asserted by its corresponding test before the
production fix is accepted. The initial vectors preserve critical malformed-input and
boundary cases already covered by the campaign.

# Generated by ark-check --install-agent-gates (Grok Build project scope).
# This repo IS Ark — run local bin so the working tree is the gate source of truth.
# Restart Grok (or /mcps → refresh) after changes. Also loads repo-root .mcp.json.
[mcp_servers.ark]
command = "node"
args = ["bin/ark-mcp.mjs", "--root", ".", "--config", "ark.config.json"]

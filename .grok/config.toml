# Generated by structrail-check --install-agent-gates (Grok Build project scope).
# This repo IS Structrail — run the local bin so the working tree is the gate source of truth.
# Restart Grok (or /mcps → refresh) after changes. Also loads repo-root .mcp.json.
[mcp_servers.structrail]
command = "node"
args = ["bin/structrail-mcp.mjs", "--root", ".", "--config", "structrail.config.json"]

# ArkGate 4.5.5

- **Status:** published
- **npm:** `arkgate@4.5.5` (`latest`)
- **Git tag:** `v4.5.5`
- **OIDC publish:** `publish-npm.yml` run `31449980409`
- **gitHead:** `6c31dbf`
- **PR:** #126 squash `6c31dbf`

**Patch** over **4.5.0**. Ships the deep-module coach train (product vocabulary, doctor/HTML
advisory hot paths and deepening candidates, consumer glossary process hook, two-axis done)
plus **upgrade suggested improvements** so installers see concrete try/inspect actions.

No required config migration. Codex / Cursor / OpenCode remain advisory at write time. Skills
and AGENTS projection never enforce. Z09 / RB-11 remain open. No new skill names, no new
presets/packs, no LLM pass/fail in the package, no numeric architecture score.

## What ArkGate is / isn't

- **Guardrail + write firewall + coach** — not an autonomous architect. Scan is deterministic;
  process (placement, pilots, wording) is agent/human judgment outside the package.
- **Green edges / `ark-check` pass ≠ finished architecture or good design.** Design residual and
  judgment remain after Enforce; `design-weak` is honest residual, not “healthy finished.”
- **The improvement compass and deep-module coach are not scores.** Residual lenses and deepening
  candidates are projections of existing evidence; never invent green residual or fake candidates.
- **Enforce green ≠ feature done.** Architecture residual and feature/ticket residual are separate
  axes (two-axis done recipe).
- **Without a required CI / merge status check, enforcement is still avoidable.** Local hooks and
  MCP only coach; they do not replace a required GitHub status context.
- **Soft hosts (Codex / Cursor / OpenCode):** advisory at local write. The hard merge boundary is a
  **required** GitHub status running `arkgate-check --strict-merge`.

## What changed

| Area | 4.5.5 result |
|------|--------------|
| Deep-module coach | Doctor + HTML `deepModuleCoach`: hot paths (git heuristic, `unavailable` if no history) + deepening candidates from existing smells/cohesion/compass only; always `notAScore` |
| Product vocabulary | Seam / depth / deletion test / test-at-public-interface in product-voice + skills (explore, think, place, fix, loop, adopt) |
| Two-axis done | Agent-guide + compact router: architecture residual vs feature residual; Enforce green ≠ feature done |
| Domain glossary | Adopt/explore process: prefer consumer glossary terms when present; missing is normal |
| Upgrade what’s new | `ark upgrade` JSON `whatsNew` + human block: try/inspect for coach, compass, session honesty, two-axis done, self-service honesty |
| Freezes | No scores, no new skill names, no new sensors, no LLM package verdict |

## What did not change

- Write gate / CI fail-closed semantics for value-layer edges
- Host hardness claims (Codex/Cursor/OpenCode stay advisory at local write)
- Skill **names** (still 13)
- Z09 / RB-11 field claims (**still open**)
- Binary gate; no numeric trust score; no LLM process verdict in package
- No new ArkRules sensors
- Coach / compass residual never flips `valid` / strict-merge / `goal.met`

## Upgrade

```bash
npm install -D arkgate@4.5.5
# or after publish: npm install -D arkgate@latest
npx arkgate upgrade --json          # selfService + whatsNew suggestions
npx arkgate-check --doctor          # deepModuleCoach + improvementCompass
npx arkgate status --json           # session honesty modes
```

No `ark.config.json` migration. Dual bins `arkgate*` / `ark*` unchanged.

### After upgrade — suggested try list

1. **Doctor deep-module coach** — hot paths + deepening candidates (`doctor.deepModuleCoach`)
2. **Improvement compass** — residual lenses (`doctor.improvementCompass`, not a score)
3. **Session recipe** — bind identity → `ark status --json` → act; doctor when mode is not full
4. **Two-axis done** — architecture residual vs feature/ticket acceptance (Enforce green ≠ done)
5. **Self-service honesty** — upgrade `selfService` write-path labels + customized preserve

Product paths: [use.md](../use.md) · [agent-guide — Two-axis done](../agent-guide.md) ·
[package surface](../package-surface.md).

## Compatibility and limits

- Deepening candidates empty when no residual evidence — never fake list.
- Hot paths `unavailable` when git history is missing — never invent paths.
- Status residual and coach are advisory coaching, not gate inputs.
- Soft hosts stay advisory; required CI status remains the shared hard merge boundary.
- Does not close Z09 / RB-11 retained-adoption claims.

## Maintainer publication checklist

1. [x] Full-matrix CI and Security green on the release SHA
2. [x] Package measurement under budget ceilings (or re-record budget)
3. [x] `npm run release:npm -- --dry`
4. [x] Signed tag `v4.5.5` + GitHub Release from this file
5. [x] `gh workflow run publish-npm.yml -f tag=v4.5.5 -f dry_run=false` (OIDC)
6. [x] Verify npm version, `latest`, provenance, and `gitHead`
7. [x] Flip public pointers and this status to **published**
8. [x] `mcp-publisher validate server.json && mcp-publisher publish server.json`

## Security notes

No new dependency advisories unique to this prepare beyond overrides already on main.
Deep-module coach, upgrade what’s-new, and process vocabulary add advisory surfaces only — they do
not weaken gate edges or invent hard-write for soft hosts.

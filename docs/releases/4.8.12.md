# ArkGate 4.8.12

- **Status:** prepared (tree candidate; npm `latest` remains **4.8.11**)
- **npm:** `arkgate@4.8.12` (not published)
- **Git tag:** `v4.8.12` (not created)
- **Prior published:** `arkgate@4.8.11`

**Patch** over **4.8.11**. First-contact honesty for optional ArkOrder: doctor,
start, status, and the check label say the extra in plain words. Sensors and
`arkgate/order` already shipped. **No required config migration.** No
`schemaVersion` bump. Does **not** close `K01` / `Z09`. No new skill names.
Mother `ark.config.json` stays extras-off. No ξ / Haken on the first screen.

## What changed

- Doctor / HTML / status show an ArkOrder section when the extra is on (`notAScore`).
- Check prints `[ArkOrder]` on `ARKORDER_*` denies.
- `ark start` says extras stay off until you turn them on.
- First-contact copy uses the one-minute breath: plan vs seats, valve not update.

## Upgrade

```bash
npm install -D arkgate@4.8.12
npx arkgate upgrade --json
npx arkgate-check --doctor
```

No required config migration.

After merge: signed tag `v4.8.12`, then `gh workflow run publish-npm.yml`.

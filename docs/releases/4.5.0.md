# ArkGate 4.5.0

- **Status:** published
- **npm:** `arkgate@4.5.0` (`latest`)
- **Git tag:** `v4.5.0`
- **OIDC publish:** `publish-npm.yml` run `31434961289`
- **gitHead:** `d6f4359`
- **PR:** #123 squash `d6f4359`

**Minor** over **4.4.0**. Session control-plane honesty: status and MCP project improvement-compass
residual with explicit modes (`full` | `subset` | `unavailable`), domain module fitness (split
oversized pure modules instead of only raising budgets), selective pure verification ratchet,
self-service upgrade activation honesty, and a short **session recipe** in product docs.

No required config migration. Codex / Cursor / OpenCode remain advisory at write time. Skills
and AGENTS projection never enforce. Z09 / RB-11 remain open. No new skill names, no new
presets/packs, no LLM pass/fail in the package, no numeric architecture score.

## What ArkGate is / isn't

- **Guardrail + write firewall + coach** — not an autonomous architect. Scan is deterministic;
  process (placement, pilots, wording) is agent/human judgment outside the package.
- **Green edges / `ark-check` pass ≠ finished architecture or good design.** Design residual and
  judgment remain after Enforce; `design-weak` is honest residual, not “healthy finished.”
- **The improvement compass is not a score.** Residual lenses are projections of existing
  evidence; status honesty modes never invent green residual when facts are incomplete.
- **Structure sensors and many smells are heuristics.** Validate warnings by reading the code.
- **Without a required CI / merge status check, enforcement is still avoidable.** Local hooks and
  MCP only coach; they do not replace a required GitHub status context.
- **Soft hosts (Codex / Cursor / OpenCode):** advisory at local write. The hard merge boundary is a
  **required** GitHub status running `arkgate-check --strict-merge` (alias `ark-check --strict-merge`).

## What changed

| Area | 4.5.0 result |
|------|--------------|
| Status / MCP compass | `ark status --json` / `ark_status` project `improvementCompass` with mode `full` \| `subset` \| `unavailable`; residual ids ⊆ doctor when `full`; always `notAScore` |
| Session recipe | Product docs: bind identity → status → act; run doctor when status mode is not full |
| Domain fitness | Oversized pure `improvementCompass` split into types + map + facade; module budgets require split when over ceiling |
| Pure verification | Selective property/mutation ratchet on peerIsolation fail-closed, policy-delta ack match, invariant promote honesty |
| Upgrade self-service | `ark upgrade` surfaces activation labels + customized content preserve (`selfService`) without a maintainer ticket |
| Public docs | use / agent-guide / develop teach session recipe and honesty modes in product language |

## What did not change

- Write gate / CI fail-closed semantics for value-layer edges
- Host hardness claims (Codex/Cursor/OpenCode stay advisory at local write)
- Skill **names** (still 13)
- Z09 / RB-11 field claims (**still open**)
- Binary gate; no numeric trust score; no LLM process verdict in package
- No new ArkRules sensors
- Compass residual never flips `valid` / strict-merge / `goal.met`

## Upgrade

```bash
npm install -D arkgate@4.5.0
# or after publish: npm install -D arkgate@latest
npx arkgate status --json
npx arkgate-check --doctor
npx arkgate-check --doctor --json   # doctor.improvementCompass
npx arkgate upgrade --json          # selfService activation + preserve
```

No `ark.config.json` migration. Dual bins `arkgate*` / `ark*` unchanged.

### Session recipe (after upgrade)

1. Bind identity (`ark_identity` or `ark status --expected-root …`)
2. Read `ark status --json` / MCP `ark_status`
3. Act on residual / next action / `findingRef`
4. If compass `mode` is `subset` or `unavailable`, run doctor for the full residual map

Product path: [use.md — Session recipe](../use.md#session-recipe-agent-turn).

## Compatibility and limits

- Status residual is advisory coaching, not a gate input.
- Incomplete status facts never invent green residual lenses.
- Soft hosts stay advisory; required CI status remains the shared hard merge boundary.
- Managed upgrade self-service is advisory and never invents hard-write from disk alone.
- Does not close Z09 / RB-11 retained-adoption claims.

## Maintainer publication checklist

1. [x] Full-matrix CI and Security green on the release SHA (PR #123 / squash `d6f4359`)
2. [x] Package measurement under budget ceilings (or re-record budget)
3. [x] `npm run release:npm -- --dry`
4. [x] Signed tag `v4.5.0` + GitHub Release from this file
5. [x] `gh workflow run publish-npm.yml -f tag=v4.5.0 -f dry_run=false` (OIDC run `31434961289`)
6. [x] Verify npm version, `latest`, provenance, and `gitHead` (`d6f4359`)
7. [x] Flip public pointers and this status to **published** (PR #124 / `656de82`)
8. [x] `mcp-publisher validate server.json && mcp-publisher publish server.json`

## Security notes

No new dependency advisories unique to this prepare beyond overrides already on main.
Status honesty, upgrade self-service, and session recipe docs add advisory surfaces only — they do
not weaken gate edges or invent hard-write for soft hosts.

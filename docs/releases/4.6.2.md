# ArkGate 4.6.2

- **Status:** published
- **npm:** `arkgate@4.6.2` (`latest`)
- **Git tag:** `v4.6.2`
- **OIDC publish:** `publish-npm.yml` run `31984939719`
- **gitHead:** `7aabe4a`
- **PR:** #141
- **Target package:** `arkgate@4.6.2`

**Patch** over **4.6.1**. First-contact copy so a newcomer (human or coding agent) knows
the next move in a few seconds. Same 13 skill names. No new scores, sensors, or doors.

No required config migration. **ArkGate** and **ArkRules** stay as product names. Compass is
still not a score. Soft hosts stay advisory at write except Cursor’s existing Write/StrReplace
pre-write block.

## What ArkGate is / isn't

- **Guardrail + write firewall + coach** — not an autonomous architect.
- **Green imports / `ark-check` pass ≠ finished architecture or good design.**
- **The improvement compass is not a score.**
- **Enforce green ≠ feature done.**
- **Invoking a door is the approval.** The CLI does not apply the change for you.
- **Law files are a different change type** than product source.

## What changed

| Area | 4.6.2 result |
|------|--------------|
| Setup help | `arkgate` (alias `ark`) — config / check / coach; start → doctor → `/ark-adopt` |
| Check help | `arkgate-check --doctor` and `--strict-merge` on the first screen; `--help --all` for the rest |
| Doctor | Light + primary next action #1 first; honesty / compass / fitness after |
| Deny | `blocked {file} — {reason}` / `Next: move the import or /ark-place` / `[RULE_ID]` |
| SessionStart | `New to Ark? /ark-adopt or: arkgate-check --doctor` |
| MCP | `ark_identity` first; `ark_check` is a scan, not a score; `server.json` is the layers sentence |
| Skills | Five doors: when + steps. Other 8 names stay shortcuts, not the menu |
| Status | Leftover design work → `map-leftover-design`, never `stay-enforced` |
| npm description | `One architecture config. One check. One coach.` |
| Freezes | No scores, no new skill names, no package LLM verdict, no fake hard-write |

## What did not change

- Write gate / CI semantics for value-layer import rules
- Codex / OpenCode remain warning-only at local write
- Skill **names** (still 13)
- Z09 / RB-11 field claims (**still open**)
- Binary gate; no numeric trust score; no LLM process verdict in package
- No new ArkRules sensors
- Five-door autonomy and team parliament from 4.6.1

## Upgrade

```bash
npm install -D arkgate@4.6.2
# after publish: npm install -D arkgate@latest
npx arkgate-check --install-agent-gates --skills-only --force
npx arkgate-check --doctor
```

No `ark.config.json` migration. Dual bins `arkgate*` / `ark*` unchanged.

## Compatibility and limits

- Does not invent hard-write for Codex or OpenCode.
- Does not close Z09 / RB-11 retained-adoption claims.
- GitHub repo About still needs a maintainer `gh repo edit` (this environment got 403).

## Maintainer publication checklist

1. [x] Land PR to protected `main`
2. [x] Signed tag `v4.6.2` + GitHub Release from this file
3. [x] `gh workflow run publish-npm.yml -f tag=v4.6.2 -f dry_run=false` (OIDC)
4. [x] Flip this file + CHANGELOG **Status: published**
5. [x] `mcp-publisher validate server.json && mcp-publisher publish server.json` (`io.github.pedroknigge/arkgate@4.6.2`)
6. [x] arkgate-site changelog/4.6.2 (first-contact copy)
7. [x] Set GitHub About to `One architecture config. One check. One coach.`

## Prepare audit

| Surface | Ready |
|---------|-------|
| `package.json` / lock / `server.json` / `src/version.ts` → 4.6.2 | yes |
| CHANGELOG 4.6.2 **published** (npm `latest`) | yes |
| Release notes Status **published** | yes |
| Public “current published” **4.6.2** | yes |
| First-contact help / doctor / deny / SessionStart / doors | yes |
| q06 CURRENT=4.6.2 · PUBLISHED_LATEST=4.6.2 | yes |

## Security notes

No new dependency advisories unique to this prepare beyond overrides already on main.
First-contact copy does not weaken import-edge fail-closed semantics or invent hard-write
for soft hosts.

# ArkGate 4.7.2

- **Status:** published
- **npm:** `arkgate@4.7.2` (`latest`)
- **Git tag:** `v4.7.2`
- **Prior published:** `arkgate@4.7.1`

**Patch** over **4.7.1**. Public copy uses ordinary software words. **No required config migration.**
Companion `@arkgate/runtime` is not republished.

Does not close Z09 / RB-11. Does not close K01.

## What ArkGate is / isn't

- If the AI writes an illegal import, the write is rejected. The same check fails the pull
  request. Not an API Gateway. Not a folder linter. If the check is not required on the PR,
  the rules file is just documentation.
- **Skills never enforce.** The hook, the check, and required CI do.
- **ArkRules** and **ArkRun** extras are off unless you turn them on.

## What changed

| Area | 4.7.2 result |
|------|----------------|
| First-contact copy | Deny → not-that → how. One rules file. One check. One next step. |
| npm `description` | The deny sentence |
| Compact router / doctor / skills picker | Ordinary software words on the first screen |
| Action / MCP `server.json` | Same deny sentence |

## What did not change

- Layers / ArkRules / ArkRun sensors and merge teeth
- Skill **names** (still 13)
- Binary check; no numeric score; no LLM process verdict
- Companion `@arkgate/runtime` (not republished by this patch)
- ADR 0001 product title
- Historical 4.6.2 three-beat line
- Z09 / RB-11 field claims (**still open**)
- K01 in-process commit gaps (**still parked**)

## Upgrade

```bash
npm install -D arkgate@4.7.2
npx arkgate upgrade --json
npx arkgate upgrade --apply
npx arkgate-check --doctor
```

```bash
mcp-publisher validate server.json
```

## Compatibility and limits

- No `ark.config.json` migration.
- Host write hardness is unchanged from 4.7.1.

## Maintainer publication checklist

1. [x] Land the 4.7.2 commit on protected `main`
2. [x] Signed tag `v4.7.2` + GitHub Release from this file
3. [x] `gh workflow run publish-npm.yml -f tag=v4.7.2 -f dry_run=false`
4. [x] Verify npm `latest`, provenance, checksum, and `gitHead`
5. [x] `mcp-publisher validate server.json && mcp-publisher publish server.json`
6. [x] Flip public pointers and publish arkgate-site 4.7.2

## Security notes

This release does not weaken architecture policy, expand filesystem access, auto-apply
reshapes, or change CI fail-closed semantics.

# ArkGate 4.1.1

- **Status:** published
- **npm:** `arkgate@4.1.1` (superseded by `4.2.0` on `latest`)
- **Git tag:** `v4.1.1`

**Patch** over **4.1.0**. Phase EH — enforcement evidence modeling + documentation truth from
Codex × ArkGate field feedback (2026-07-25) plus a public-lane claims audit.

No required config migration. Does not invent hard-write for Codex/Cursor/OpenCode. Does not close
Z09 / RB-11 field claims. Does not add a numeric architecture score.

## What changed

| Area | 4.1.1 result |
|------|--------------|
| Product honesty (EH05) | Soft-write host alone no longer forces global **Not finished**; composite headline + environment residual bucket; `contractReadiness` / `localWriteBoundary` |
| Provider policy (EH06) | GitHub plan HTTP 403 → `provider-policy-unavailable-plan` / `unavailable-plan` |
| CI runtime (EH06) | Successful Actions runs can show `runtimeObserved: true` independent of branch-protection API; `hard: false` when status not required |
| Report `.gitignore` (EH03) | Treats `.ark/*` / reports paths as coverage; never broadens `.ark/` over `!` exceptions |
| CI base-ref (EH04) | First push (all-zero `before`) skips `--fail-on-new-smells` delta; keeps full `--strict-merge` |
| Repair / host matrix (EH07) | Envelope emission vs reinjection guarantee; CLI vs GitHub status context wording; Codex ops matrix hard=false |
| Docs truth (EH01–EH02, EH08) | Claims matrix 2026-07-25; public lanes 0 Contradicted; product-voice + ai-gates + README sync |

## What did not change

- Write gate / CI fail-closed semantics for **value** layer edges
- ArkRules opt-in semantics
- Host hardness claims (Codex/Cursor/OpenCode stay advisory at write)
- Z09 / RB-11 field claims (**still open**)
- Package budget measured candidate remains the 4.0.0 clean candidate unless pack exceeds ceilings

## Upgrade

```bash
npm install -D arkgate@4.1.1
# or: npm install -D arkgate@latest
npx arkgate-check --doctor --json
# optional GitHub provider evidence:
ARK_DOCTOR_GITHUB=1 npx arkgate-check --doctor --json
```

No config migration. Dual bins `arkgate*` / `ark*` unchanged.

## Maintainer publication checklist

1. [x] CI green on the release SHA  
2. [x] `npm run release:npm -- --dry`  
3. [x] Signed tag `v4.1.1` + GitHub Release from this file  
4. [x] `gh workflow run publish-npm.yml -f tag=v4.1.1 -f dry_run=false`  
5. [x] Verify: `npm view arkgate@4.1.1 version` and `npm view arkgate dist-tags.latest` → `4.1.1`  
6. [x] Flip CONTRIBUTING / README / this status to **published**  
7. [x] `mcp-publisher publish server.json` (GitHub token login)

## Security notes (honest)

No dependency CVE bumps unique to this prepare. Behavior changes are honesty taxonomy, CI
template base-ref safety, and report gitignore mutation — not a silent weaken of gate edges.
